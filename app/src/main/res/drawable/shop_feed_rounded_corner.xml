<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- view background color -->
    <solid android:color="@color/shopFeedItemBackgroundColor" >
    </solid>

    <!-- view border color and width -->
    <stroke
        android:width="@dimen/shop_feed_item_border_width"
        android:color="@color/shopFeedItemBorderColor" >
    </stroke>

    <!-- Here is the corner radius -->
    <corners android:radius="@dimen/shop_feed_item_corner_radius" >
    </corners>

</shape>